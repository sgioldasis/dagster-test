# PROD Environment Configuration - Databricks
source: POSTGRES_CONN
target: DATABRICKS_TEST

defaults:
  mode: full-refresh
  object: "{stream_table}"
  replication_key: updated_at

streams:
  public.raw_customers:
    object: "main.raw_customers"
  # public.raw_orders:
  #   object: "main.raw_orders"
  # public.raw_payments:
  #   object: "main.raw_payments"