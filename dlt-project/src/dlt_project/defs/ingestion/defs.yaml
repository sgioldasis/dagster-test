type: dagster_dlt.DltLoadCollectionComponent

attributes:
  loads:
    - source: .loads.target_data
      pipeline: .loads.pipeline
      # Optional: add translation for customizing assets
      translation:
        group_name: ingestion
        key_prefix:
          - target
        description: "Loads target main data into DuckDB"
