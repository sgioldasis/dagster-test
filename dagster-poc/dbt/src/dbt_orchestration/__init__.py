"""DBT Cloud orchestration package."""

__version__ = "0.1.0"

# Note: defs and dbt_defs are not auto-imported to avoid heavy dependencies
# Use: from dbt_orchestration.definitions import defs, dbt_defs
