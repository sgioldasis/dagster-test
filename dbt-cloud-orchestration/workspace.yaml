load_from:
  # Ingestion Code Location - DLT Databricks pipelines
  - python_module:
      module_name: "dbt_cloud_orchestration.defs.ingestion.definitions"
      location_name: "ingestion"

  # DBT Code Location - DBT Cloud assets and jobs
  - python_module:
      module_name: "dbt_cloud_orchestration.defs.dbt.definitions"
      location_name: "dbt"

  # Downstream Code Location - Downstream processing assets
  - python_module:
      module_name: "dbt_cloud_orchestration.defs.downstream.definitions"
      location_name: "downstream"